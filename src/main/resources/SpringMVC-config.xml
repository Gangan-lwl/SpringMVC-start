<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:content="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
            http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context
            http://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/mvc
            http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 静态资源访问配置 -->
    <mvc:resources mapping="/css/**" location="static/css/"/>
    <mvc:resources mapping="/img/**" location="static/img/"/>
    <mvc:resources mapping="/js/**" location="static/js/"/>

    <!-- SpringMVC 包扫描器配置，配合 @Controller 使用，扫描使用了注解的包 -->
    <content:component-scan base-package="com.icarus.controller"/>

    <!-- SpringMVC 视图解析器配置 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!-- 设置视图前缀 -->
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <!-- 设置视图后缀 -->
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 数据格式转换器配置 -->
    <mvc:annotation-driven conversion-service="conversionService">
        <mvc:message-converters register-defaults="false">
            <!-- JSON 格式转化器 -->
            <bean class="com.alibaba.fastjson.support.spring.FastJsonHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value> text/html; charset=UTF-8 </value>
                        <value> application/json; charset=UTF-8 </value>
                    </list>
                </property>
            </bean>
            <!-- ByteArrayHttpMessageConverter (字节流 octet-stream) -->
            <bean class="org.springframework.http.converter.ByteArrayHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value> application/octet-stream; charset=UTF-8 </value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 自定义数据格式转换器配置 -->
    <!-- 字符串日期转换器 -->
    <bean class="org.springframework.context.support.ConversionServiceFactoryBean">
        <property name="converters">
            <set>
                <bean class="com.icarus.converter.DateConverter"/>
            </set>
        </property>
    </bean>

    <!-- 拦截器配置 -->
    <mvc:interceptors>
        <!-- 登录拦截器，禁止未登录用户访问指定路由，放行登录页面 -->
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <mvc:exclude-mapping path="/login"/>
            <bean class="com.icarus.interceptor.LoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 文件上传解析器配置 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 设置请求编码格式 -->
        <property name="defaultEncoding" value="UTF-8"/>
        <!-- 设置允许上传文件的最大值（5MB），单位 byte -->
        <property name="maxUploadSize" value="5242880"/>
    </bean>

</beans>